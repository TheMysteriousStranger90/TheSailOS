using System;
using CosmosFtpServer;

namespace TheSailOS.NetworkTheSailTwo;

public class TheSailFTP
{
    public static void StartFtpServer(FileSystem fs, string rootPath)
    {
        /*
        try
        {
            using (var ftpServer = new FtpServer(fs, rootPath))
            {
                Console.WriteLine("Starting FTP Server...");
                ftpServer.Listen();
            }
        }
        catch (Exception ex)
        {
            Console.WriteLine($"[ERROR] FTP Server failed: {ex.Message}");
        }
        */
    }
}